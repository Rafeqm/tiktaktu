name: tiktaktu
description: Play with whoever you want.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 0.5.0-beta.1+2

environment:
  sdk: ">=2.16.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
